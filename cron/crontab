##
# We're going to load the environment variables into our intermediary script
#

* * * * * BASH_ENV=/var/www/html/.env /opt/scripts/runcron.sh >> /var/log/cron.log 2>&1

# Cron files must have an ending blank line
